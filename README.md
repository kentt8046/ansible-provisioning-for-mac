# ansible-provisioning-for-mac

```bash
$ /usr/bin/ruby -e "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/master/install)"
$ brew update
$ brew install ansible
$ ansible-playbook -vv localhost.yml
```
